// Typed Negatic API client.
//
// This package will be (re)generated from ../../api-spec/openapi.yaml using
// openapi-typescript or similar. For now it's an empty placeholder so the
// workspace graph resolves; the dashboard and bot can import from
// @negatic/shared-schemas in the meantime.

export {};
