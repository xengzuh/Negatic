# @negatic/api-client

Generated typed client for the Negatic API. Source: `api-spec/openapi.yaml`.

Currently a placeholder — generation is wired up in a follow-up task. Until
then, consumers should import runtime schemas from `@negatic/shared-schemas`
and call PostgREST endpoints with `fetch` directly.
